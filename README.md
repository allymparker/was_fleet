# Demo

Multi Cluster
Multi Team
onboard new tenant
fail to deploy to another ns
fail to go over RQ


flux bootstrap github \
    --context=kind-dev \
    --owner=allymparker \
    --repository=was_fleet \
    --branch=main \
    --personal \
    --path=clusters/dev
